---
kind: Quest
spec:
  name: Corrupted User
  description: "A corrupted user has stolen some vital hardware. DO NOT DELETE! The hardware must be recovered" 
  location: [34, -32, -2]
  tasks:
    - kind: message
      name: Have strength + defence > 50
      message: failCredentials
      buildingKind: Control Tower
    - kind: message
      name: Have life > 100
      message: failCredentials
      buildingKind: Control Tower
    - kind: message
      name: Delete a Paperclip Maximizer
      message: failCredentials
      buildingKind: Control Tower
