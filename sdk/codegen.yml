overwrite: true
schema: ../contracts/lib/cog/services/schema/*.graphqls
documents: "src/schema/*.graphql"
generates:
  src/schema/gql:
    preset: client
    plugins: []
hooks:
  afterAllFileWrite:
    - prettier --write
    - eslint --fix
